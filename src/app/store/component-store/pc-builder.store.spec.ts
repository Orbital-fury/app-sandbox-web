import { PcBuilderStore } from './pc-builder.store';

describe('PcBuilderStore', () => {
  const componentStore = new PcBuilderStore();

  it('should be created', () => {
    expect(componentStore).toBeTruthy();
  });
});
